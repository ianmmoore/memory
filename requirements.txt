# Core dependencies
# sqlite3 is built-in with Python

# Testing (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0

# LLM integration
openai>=1.0.0
pyyaml>=6.0.0
python-dotenv>=1.0.0

# Terminal-Bench (requires Python >= 3.12)
# Install manually: pip install terminal-bench
# terminal-bench>=0.1.0

# Anthropic (Claude) - optional
# anthropic>=0.5.0

# For type checking (development)
# mypy>=1.0.0
